import { EDisplayTypes } from "../redux";

export const POINT_DISPLAY_TYPES = [EDisplayTypes.POINTS, EDisplayTypes.MIX];
export const GRID_DISPLAY_TYPES = [EDisplayTypes.GRID, EDisplayTypes.MIX];
export const NON_GRID_DISPLAY_TYPES = [EDisplayTypes.POINTS, EDisplayTypes.HEATMAP];